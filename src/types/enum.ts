/**表单成员类型*/
export enum EnFormItemType {
  INPUT = 0,
  SELECT,
  DATE,
  TIME,
  SWITCH,
  CHECKBOX,
  RADIO,
}

/**白名单页面*/
export enum EnWhiteList {
  LOGIN = '/login',

}

export enum EnTest {
  TEST = 0,
  PPP
}
